import React from 'react';
import PlaceholderPage from '../PlaceholderPage';

const Backtest: React.FC = () => {
  return <PlaceholderPage title="回测中心" description="策略回测管理：创建回测任务、查看回测结果。" />;
};

export default Backtest;
